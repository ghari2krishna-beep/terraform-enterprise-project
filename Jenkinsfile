pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checkout successful'
            }
        }

        stage('Build') {
            steps {
                echo 'Building application'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application'
            }
        }
    }
}
